export enum UserRoleEnum {
  Student = 'student',
  Teacher = 'teacher',
  Admin = 'admin',
  Bureau = 'bureau',
}

export enum GenderEnum {
  Masculin = 'masculin',
  Feminin = 'féminin',
}

export enum UserType {
  Student = 'student',
  Parent = 'parent',
  Both = 'both',
}
